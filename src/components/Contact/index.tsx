import { SelectedPage } from "@/shared/types";


type Props={
    setSelectedPage: (value:SelectedPage) => void
  };

  
const Contact = ({setSelectedPage}:Props) => {
    return (
   <div>

    TODO: add Contact stuff here 
   </div>
  );
};

export default Contact;